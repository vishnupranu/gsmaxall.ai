# GSMAXALL — `analytics` service

Usage and event metering

## Run locally
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8089
# health: http://localhost:8089/health
```
